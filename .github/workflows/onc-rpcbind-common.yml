name: onc-rpcbind-common CI
on:
  push:
    branches: [main]
    paths:
      - "packages/**"
      - ".github/workflows/onc-rpcbind-common.yml"
      - ".github/workflows/module.yml"

permissions:
  id-token: write
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/onc-rpcbind-common

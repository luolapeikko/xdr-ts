name: onc-node CI
on:
  push:
    branches: [main]
    paths:
      - "packages/**"
      - ".github/workflows/onc-node.yml"
      - ".github/workflows/module.yml"

permissions:
  id-token: write
  contents: read

jobs:
  module-flow:
    uses: ./.github/workflows/module.yml
    with:
      package-path: packages/onc-node
